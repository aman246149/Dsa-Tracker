import 'package:flutter/material.dart';

class Striver extends StatefulWidget {
  const Striver({ Key? key }) : super(key: key);

  @override
  _StriverState createState() => _StriverState();
}

class _StriverState extends State<Striver> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}